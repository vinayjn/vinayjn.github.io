machine:
  environment:
    USER_NAME: vinayjn
    USER_EMAIL: vinay.jn7@gmail.com
  ruby:
    version: 2.3.1

dependencies:
  pre:
    - ./bin/setup

test:
  post:
    - bundle exec jekyll build
    - bundle exec htmlproofer ./_site --only-4xx --allow-hash-href

deployment:
  production:
    branch: master
    commands:
      - ./bin/automated